# Supabase Configuration
# Get these values from your Supabase project dashboard
# https://app.supabase.com/project/_/settings/api
# 
# Note: Use ANON KEY (public key) for frontend, NOT service role key!

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: These are public environment variables that will be exposed in the client bundle.
# Never put sensitive keys (like service role key) in frontend environment variables.
